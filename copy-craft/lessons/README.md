# Lessons

One principle per file. Template and add-rules live in `../README.md`.

Filename: `YYYY-MM-DD-slug.md`

Required headings: Source, Date added, Principle, Popsmith failure mode, One line we might run, Where this fails on Popsmith.
